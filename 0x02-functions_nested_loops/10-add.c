#include <stdio.h>
#include "holberton.h"

/**
 *add-function - adds two integers and returns the result.
 *@a:number that is goint to be add to another number
 *@b:the other number.
 * Return: a plus b the addition of 2 numbers.
 */

int add(int a, int b)
{
	return (a + b);
}
