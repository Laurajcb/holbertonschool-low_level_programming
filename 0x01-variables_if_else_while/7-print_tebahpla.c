#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 * Void always return: 0
 * Return: 0
 */

int main(void)
{
	int n;
for (n = 122; n >= 97; n--)
{
putchar(n);
}
putchar('\n');

return (0);
}
