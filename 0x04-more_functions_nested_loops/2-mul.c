#include <stdio.h>
#include "holberton.h"

/**
 * mul- Function that multiplies two integers.
 * @a:number.
 * @b:number
 * Return: the resul of multiplies a * b.
 **/

int mul(int a, int b)
{
	int result;

	result = (a * b);
	return (result);
}
